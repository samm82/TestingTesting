\input{common_preamble}

% Enable links within the document
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=red,
    urlcolor=red,
    breaklinks=true
}
\urlstyle{rm} % Make URL styled fonts match hyperref's hrefs
\usepackage[nameinlink]{cleveref} % Fixes capitalization of internal references

% General Utility Functions
\input{paper_macros}

% \usepackage{cite}
\usepackage[square,sort,comma,numbers]{natbib}
\newcommand{\citetISTQB}{\cite{ISTQB}}
\newcommand{\citepISTQB}{\cite{ISTQB}}
\newcommand{\citealpISTQB}{\cite{ISTQB}}
\renewcommand{\citeyearpar}{\cite}
\renewcommand{\citeyear}{\citealp}
\renewcommand{\citet}{\cite}

\usepackage{algorithmic}
\usepackage{textcomp}

\usepackage[disable]{todonotes}

\newenvironment{paperTable}{
    \begin{table*}[t]
        }{
    \end{table*}
}

\newenvironment{paperFigure}{
    \begin{figure*}[t]
        }{
    \end{figure*}
}